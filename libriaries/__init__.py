"""Compatibility package for legacy imports.

The original project used the folder name `libriaries`.  It is kept here so old
imports continue to work, but all paths are now standardized through
`input/` and `output/`.
"""
